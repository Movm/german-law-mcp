# Optional: limit loaded adapters to specific countries.
# Example: LAW_COUNTRIES=de
LAW_COUNTRIES=

# Required by docker-compose.yml. Use a long random value.
POSTGRES_PASSWORD=

# Required snapshot tag from QuantLaw/gesetze-im-internet (YYYY-MM-DD).
QUANTLAW_SNAPSHOT=

# Optional but strongly recommended for a private deployment.
MCP_BEARER_TOKEN=

# Optional comma-separated Origin allowlist, for example:
# ALLOWED_ORIGINS=https://claude.ai,https://chatgpt.com
ALLOWED_ORIGINS=

# Optional: SQLite path for ingested German corpus.
# Example: GERMAN_LAW_DB_PATH=/absolute/path/to/database.db
GERMAN_LAW_DB_PATH=

# Optional: cap ingestion size when invoking law.run_ingestion.
# Example: GERMAN_LAW_INGEST_MAX_LAWS=100
GERMAN_LAW_INGEST_MAX_LAWS=

# Optional: cap case-law ingestion size when invoking law.run_ingestion
# with sourceId=rechtsprechung-im-internet.
# Example: GERMAN_LAW_INGEST_MAX_CASES=5000
GERMAN_LAW_INGEST_MAX_CASES=
GERMAN_LAW_INGEST_CASES_STOP_AFTER_EXISTING=

# Optional: cap preparatory-works ingestion size when invoking law.run_ingestion
# with sourceId=dip-bundestag.
# Example: GERMAN_LAW_INGEST_MAX_PREP_WORKS=10000
GERMAN_LAW_INGEST_MAX_PREP_WORKS=
GERMAN_LAW_INGEST_PREP_STOP_AFTER_EXISTING=

# Optional: comma-separated Bundestag Wahlperioden used for DIP ingestion.
# Example: GERMAN_LAW_PREP_WAHLPERIODEN=20,19
GERMAN_LAW_PREP_WAHLPERIODEN=20

# Optional: DIP API key override. If unset, the ingestor will attempt to read
# the public key exposed via dip-config.js.
GERMAN_LAW_DIP_API_KEY=

# Optional defaults for automated update cycles (scripts/auto_update_german_law.py).
# Example: keep updates incremental and fast.
GERMAN_LAW_AUTO_UPDATE_CASES_MAX=1000
GERMAN_LAW_AUTO_UPDATE_CASES_STOP_AFTER_EXISTING=500
GERMAN_LAW_AUTO_UPDATE_PREP_MAX=5000
GERMAN_LAW_AUTO_UPDATE_PREP_STOP_AFTER_EXISTING=800
